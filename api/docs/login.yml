Authentication docs
---
tags:
 - User Authentication
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This route is used to Login a new user. 
responses:
  200:
    description: login successful. Token created.
    schema:
      id: user
      properties:
        email:
          type: string
          default: a4a@example.com
        password:
          type: string
          default: paswaord
  400:
    description: Empty password or email field.